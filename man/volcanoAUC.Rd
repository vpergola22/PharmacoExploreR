% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoAUC.R
\name{volcanoAUC}
\alias{volcanoAUC}
\title{Create Volcano Plot of Gene-Drug Correlations}
\usage{
volcanoAUC(corResults, corThreshold = 0.3, pvalThreshold = 0.05)
}
\arguments{
\item{corResults}{A data frame output from \code{correlateExpressionAUC()},
containing columns: gene, cor (correlation), pval, and adj_pval.}

\item{corThreshold}{A numeric value specifying the absolute correlation
threshold for significance. Default is 0.3.}

\item{pvalThreshold}{A numeric value specifying the adjusted p-value
threshold for significance. Default is 0.05.}
}
\value{
A ggplot2 object displaying the volcano plot with threshold lines
and colored points indicating significant genes.
}
\description{
Generates a volcano plot displaying the correlation strength (x-axis)
versus statistical significance (y-axis) for all genes. This provides
a genome-wide overview of gene-drug associations, highlighting genes
that meet both correlation and significance thresholds.
}
\examples{
\dontrun{
library(PharmacoGx)

# Load data and compute correlations
pset <- downloadPSet("NCI60_2021")
drug_name <- drugNames(pset)[1]

results <- correlateExpressionAUC(
  pset = pset,
  drug = drug_name,
  sensitivity.measure = "aac_recomputed"
)

# Create volcano plot
volcano <- volcanoAUC(
  corResults = results,
  corThreshold = 0.3,
  pvalThreshold = 0.05
)

print(volcano)

# Use stricter thresholds
volcano_strict <- volcanoAUC(
  corResults = results,
  corThreshold = 0.5,
  pvalThreshold = 0.01
)
}

}
\references{
Wickham, H. (2016). ggplot2: Elegant Graphics for Data Analysis.
Springer-Verlag New York. \href{https://ggplot2.tidyverse.org}{Link}.
}
